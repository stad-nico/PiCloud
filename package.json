{
	"devDependencies": {
		"@commitlint/cli": "^19.6.1",
		"@commitlint/config-conventional": "^19.6.0",
		"eslint": "^9.19.0",
		"husky": "^9.1.7",
		"lint-staged": "^15.4.3",
		"prettier": "^3.4.2"
	},
	"scripts": {
		"prepare": "husky",
		"openapi:generate" : "cd backend && npm run openapi:generate && cd ../frontend && npm run openapi:generate"
	}
}
